includes:
  - base.yaml
# openvino benchmark
targets:
  # copy benchmark_app
  - target: /bin
    copy:
      - files:
        - ../openvino_src/bin/intel64/Release/benchmark_app
  # copy plugins.xml
  - target: /lib
    copy:
      - files:
        - ../openvino_src/bin/intel64/Release/lib/plugins.xml
  # copy models
  - target: /
    copy:
      - files:
        - ../model